{
  "name": "reader-sideload-library",
  "version": "0.1.6",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.app.config.ts",
    "dev:site": "vite --config vite.site.config.ts",
    "tauri": "tauri",
    "test": "npm run test:claim-inventory && npm run test:unit && npm run test:rust-core && playwright test",
    "test:e2e": "playwright test",
    "test:claims": "playwright test",
    "test:unit": "vitest run tests/*.test.ts",
    "test:claim-inventory": "node scripts/check-claims.mjs",
    "test:release": "vitest run tests/release.test.ts",
    "test:rust-core": "node scripts/check-rust-core-isolation.mjs && cargo test --manifest-path src-tauri/Cargo.toml --no-default-features --lib",
    "build:app": "tsc --noEmit && vite build --config vite.app.config.ts",
    "build:site": "npm run build:app && vite build --config vite.site.config.ts && node scripts/assemble-site.mjs",
    "build": "npm run build:site && node scripts/assemble-dist.mjs",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@fontsource/archivo-black": "^5.2.6",
    "@fontsource/ibm-plex-sans": "^5.2.7",
    "@tauri-apps/api": "2.11.1",
    "@tauri-apps/plugin-dialog": "2.7.2"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.58.2",
    "@tauri-apps/cli": "2.11.4",
    "@types/node": "22.17.2",
    "typescript": "5.9.2",
    "vite": "7.3.6",
    "vitest": "3.2.7"
  }
}
